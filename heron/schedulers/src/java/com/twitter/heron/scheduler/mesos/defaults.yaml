HERON_MESOS_FRAMEWORK_ZOOKEEPER_ENDPOINT: "master:2181"
HERON_MESOS_FRAMEWORK_ZOOKEEPER_ROOT: "/heron-mesos"
HERON_MESOS_FRAMEWORK_ZOOKEEPER_CONNECT_TIMEOUT: 30000
HERON_MESOS_FRAMEWORK_ZOOKEEPER_SESSION_TIMEOUT: 60000
HERON_MESOS_FRAMEWORK_RECONCILIATION_INTERVAL_MS: 60000
HERON_MESOS_FRAMEWORK_FAILOVER_TIMEOUT_SECONDS: 60

MESOS_MASTER_URI: "master:5050"

# default value for the config key that specifies the working directory
WORKING_DIRECTORY:       ${HOME}/.herondata/topologies/${CLUSTER}/${ROLE}/${TOPOLOGY}

BACKGROUND_SCHEDULER:    true

SCHEDULER_MESOS_HTTP_PORT:   0